# 無人実行の線引き

2026-09-05 制定。`ResearchPaper/orchestration.md` の運用規則。

**「無人」= 人が画面を見ていない時間**を指す。人が居て判断を委ねられている場合は、
本体セッションがその場で決めてよい(2026-09-05 に実際そうした)。

## 0. 無人 `git push` は**許可**(2026-09-05、ユーザー判断)

理由:コミットはすべてゲート(全体ビルド + `check.mjs` + 文字化け)を通してから push しており、
`Found/` に `sorry` を残さない規約もあるので、壊れたものが master に載る経路が実質ない。
並行セッション(ABC3b/c)も同じ運用。

## 1. 無人でやってよいこと

- `node tools/frontier.mjs` で持ち場を選ぶ
- `lean-search` / `math-planner` を走らせる(どちらも書き込まない)
- **既に方針が決まっているノード**の実装(`pgc-goal.md` のノード表にあるもの)
- 対象モジュール単体のビルド
- 節目の全体ゲート(★**実装 agent が全員止まってから**。`check.mjs` は内部で
  `lake build` を走らせるのでロックで待たされる)
- ゲートが緑なら **commit + push**
- `meta-backlog.md` / `decisions-pending.md` / `memory/` への書き込み
- `tools/lean-idioms.md` への失敗形の追記

## 2. 必ず人を待つこと(★`decisions-pending.md` に積んで、止まらず次へ進む)

- **Skeleton の statement の修理**(退化の修理)——原典の読み替えを伴う
- **経路の選択**(A/B/C のような分岐)
- **メタ提案のマージ**(隔離 worktree からの取り込み)
- **`Interface` への場の追加・削除**——巻き添え範囲が広い
- **`CLAUDE.md` / `.mcp.json` / `.claude/settings.json` / `tools/hooks/` の変更**
- 原典の**逸脱**を新たに導入するとき

★**止まらないこと。** 判断が要るものに当たったら `decisions-pending.md` に積み、
`frontier.mjs` の次の startable へ進む。着手不可ノードを飛ばすのと同じ理屈。

## 3. 停止条件(これに当たったら波を止めて人を待つ)

- `node tools/check.mjs --brief` の NG が **13 件を超えた**
- `lake build ABC3` が**2 回連続で失敗**した(1 回目は並行セッションとの衝突を疑って再実行)
- 落ちたモジュールが**自分たちの下流**である
- `node tools/graph.mjs` のノード数が想定外に動いた

## ★★★★3.5 エージェント構成（2026-09-08、★ユーザー判断。★これが最優先）

★**使ってよいのは `lean-prover` だけ。**
★★**`meta-optimizer` / `math-planner` / `general-purpose` は廃止した。配ってはならない。**
（`lean-search` / `Explore` は安い（全体の 3.5%）ので、必要なら短く使ってよい。）

★**根拠（実測、通知 238 件）**: エージェント総トークン 46,658,603 のうち
`lean-prover` 61.2% / `meta-optimizer` 15.0% / `general-purpose` 10.1% / `math-planner` 7.7%。
★**廃止した 3 種で 32.8%。**★Max プランの週間上限に対して消費が速すぎるという判断である。

★**廃止した仕事は本体が inline でやる**:
- ★**検証・測定・自分の数字の検算** —— `meta-optimizer` の 36 回の出力は、実態が「改善」ではなく
  ★**「測定器の修理と本体の数字の訂正」= 検証**だった。★これは外注する仕事ではない。
- ★**方針立て** —— 本体が原典を直接読む。
  ☆★**訂正（ユーザーの指摘で測り直した）**: 本体は当初「6 本の偽は `math-planner` 由来」と書いたが、
  ★**これは測定ではなく誤りである。**`math-planner` の最終稼働は **2026-09-06 22:43** で、
  ★**それ以降に実装 agent が 88 本走っている。★6 本はどれも `math-planner` を通っていない。**
  ★実際の出所は、メタ第 41 回の実測では **申し送り 2 件**（前の波の lean-prover の報告をそのまま次の持ち場にした）、
  ★**本体が自分で選んだ葉 1 件**、残りは**本体が前の実装者の報告から書いた次ノードの字面**である。
  ⇒ ★★**偽の出所は `math-planner` ではなく本体自身と、前の波の申し送りである。**
  ★`math-planner` を廃止する理由はトークン（7.7%）であって、偽の帰属ではない。

★**速度について正直に**: 本体は改善を示せていない。実測では 1 ノードあたり
中央 27.3 分 → 31.2 分、172k → 243k トークンと**上がっている**（交絡あり、分離できない）。
★**「エージェントを増やすと速い」という主張には裏づけが無い。**

★**持ち場（brief）は今の 1/3 の長さに削る。** 本体の brief は 2,000 語級で、
★それを 137 回払っていた。★**貼るべきは道具の実パスと禁止事項だけで、数学の見込みは要らない**
（見込みは 6 波連続で外れており、実装者は毎回「その 1 つ手前の部品」で通している）。

★**`decisions-pending.md` の記録も短くする。** 1 件 300〜800 行は本体の出力トークンの浪費である。
★残すのは ①真偽の判定 ②在庫の測定 ③次の 1 点 ④VERDICT/COST の 4 つ。

## ★★★★3.6 持ち場には「結論」ではなく「証拠の場所」を書く（2026-09-08、ユーザー判断）

★**実装者は毎回まっさらな状態で生まれる。** 受け取るのは
`.claude/agents/lean-prover.md`(~3.4k) ＋ `CLAUDE.md`(~2.2k) ＋ **本体が書く持ち場**の 3 つだけ。
★`lean-idioms.md`(285k) と `decisions-pending.md`(352k) は**通読できない**（grep のみ）。
⇒ ★★**「主張を忠実に型にする」作業の入力は、事実上「本体が書く持ち場 1 本」である。**
★そして本体は 2026-09-08 に**その主張を 20 回間違えた。**

★**書き方の規則**:
1. ★**「主張は X」と書かない。**「木の `<file:line>` が X と言っている。★**確かめよ**」と書く。
   ★実測: 20 件の偽の多くは**本体の言い換え**で生じ、実装者が原文を見て覆した。
2. ★**数学の見込みを書かない。** 本体の名指しは **19 波連続で外れ**、
   実装者は毎回「★**その 1 つ手前の部品**」で通した。書くなら「これは見込みであって測定ではない」と明記する。
3. ★**木の docstring の断定も疑うよう明記する。** 波をまたぐ知識は docstring でしか運ばれないので
   ★**誤りが伝言ゲームで増幅する**（当日 39 箇所で参照、実害 2 件が確定 ——
   「1 段では閉じない」は実は 2 段を測っていた／「木にも mathlib にも無い」は 542 行に等式で在った）。
4. ★**道具の実パスと禁止事項は毎回貼る**（§4.5。規約に書いても読まれないことが実測されている）。

☆★**なぜ fork にしないか**: 文脈を引き継ぐ `fork` は使える（費用の 99.36% は `cache_read` なので
見た目より安い可能性がある、M27）が、★**失敗の原因は文脈の量ではなく本体が書く主張の誤り**なので、
★**fork でも誤った主張ごと渡る。**★`fork` 直後の `compact` は要約の生成ぶん**純増**であり、
5 往復程度の持ち場では回収できない。★**費用は 1 度も測っていない**（測るなら `tools/session-cost.mjs`）。

## 4. 上限

★2026-09-06(メタ第 9 回 M25 / 第 10 回 M28)に**固定値をやめた**。理由は測定にある。

- ★**1 波の実装 agent の数は「配れる持ち場の数」で決める。固定値ではない。**
  `node tools/frontier.mjs`(印つき)の
  「**着手可能 かつ 保留でない かつ 空撃ちでない**」を数える。
  2026-09-06 の実測は **19 ノード中 1 件**だった(旧 §4 の上限 5 は 1 度も効いていない)。
  ★枠を埋めるために保留ノードへ agent を送らない —— 保留は**人の判断待ち**であって
  人手不足ではない。
- **`lake` / `lean.exe` を動かす agent は、独立な鎖があるとき同時 2 体まで**(main tree)。
  ★★2026-09-07 に**実験して規約を緩めた**(D27。ユーザー承認、手順はデータを見る前に固定)。
  Λ7 の Y6 と Λ6 の消費側という**本当に独立な 2 ノード**を同時に走らせた結果:

  | 項目 | 事前登録した基準 | 実測 |
  |---|---|---|
  | `check.mjs --brief` | NG 13 | ★**NG 13 × 3 回とも同一** |
  | `graph.mjs` ノード | +2 のはず | ★**2,188 × 2 回とも同一** |
  | `lean_check`(清潔な側) | 抽象核 0.05-0.6 / 具体層 0.3-2.5 秒 | **0.09-0.76 / 0.67-2.12 秒** |
  | `lean_start` | 10-25 秒(★590 秒の観測あり) | **12.0 秒** |
  | 成果物の破損 | — | ★**兆候なし** |

  ★**2026-09-06 の事故は「NG が 16 ⇄ 13 で揺れる」だったので、3 回連続で同じ数字が答えである。**

  ★**条件**(これを外したら 1 体に戻す):
  - ★**本当に独立な鎖から選ぶ**(Λ7 と Λ6 の消費側、のように)。**人工的に並べない**
  - ★**ゲートは実装 agent が全員止まってから 1 回**(この規約は変えない)
  - ★**本体自身が 3 人目の `lake` 利用者にならない**(実験中は全体ビルドを回さなかった)
  - ★**3 体以上は未検証**なので当面 2 体を上限とする

  ☆★**正直な留保 3 つ**: (1) **2 体で 1 回**の観測にすぎない。(2) ★**速くなったかは測れていない**
  —— 対照の 37.2 分/ノードは「オーケストレータの往復を含む 1 session」の値で、
  agent の実作業時間と定義が違う(M6 が未解決)。メタ第 11 回の「3 体で飽和・上限 1.3-1.44 倍」も
  **依存の構造からの推定**であって実測ではない。(3) ★**2026-09-06 の 4 件の観測が何だったのかは
  説明できていない**(別の原因 —— machine の負荷・他セッション・冷キャッシュ —— の可能性が残る)。

  ☆以下は 2026-09-06 に 1 体へ絞ったときの記録(経緯として残す):
  ★旧 §4 の例外「互いに別の新規ファイルしか触らない実装 agent は同時に走らせてよい」を
  削除した。ビルド成果物(`.ilean` / `setup.json`)は共有なので、ソースが衝突しなくても
  **測定(ゲート)が壊れる** —— 2026-09-06 に 4 件観測
  (`lean_start` 590 秒 / `leanfile.mjs` 11〜13 秒 → 1〜4 分 /
  `check.mjs` の NG が 16 ⇄ 13 で揺れる / `setup.json` が長さ 0 で読まれる)。
- **読み取り専用の agent は 5 体まで**(`frontier` / `graph` / `unwired` /
  `hedge-index` / `decl-index` / 在庫調査)。★実測で K=5 の劣化は +4〜23%、
  K=8 で +32%(論理コア 32)。★ただし `decl-index.mjs` は `.cache/*.txt` を**書く**ので、
  **同時に走らせるのは 1 体まで**にする(読む側は何体でもよい)。
- **隔離 worktree の agent は上の数に入れない**が、`lake build` を回すなら
  **冷ビルド 50 分**を覚悟すること(`meta-backlog.md` M15)。
  ★★**メタ係のゲートはこの 1 行だけ**(2026-09-07 メタ第 13 回で実測して訂正した):

  ```
  node tools/check.mjs --selftest --structured --brief
  ```

  ★★★**`--brief` だけで呼んではいけない。**`--brief` は**出力の詳しさの旗**であって段の選択ではなく、
  `check.mjs:1941` が `--brief` を除外してから `args.length === 0` を見るので
  ★**全段が走り、`--lean` 段が `lake build` を回す**。
  隔離 worktree では冷ビルドなので **50 分**かかる(M19 の実測)。
  ★段を明示すれば **1.12 秒**(PDF キャッシュ温) / **44.3 秒**(冷)。
  ★**実装 agent が走っている間は本体もこの呼び方を使う**(lake 利用者の 3 人目にならないため)。
- ★★**改善エージェント(meta-optimizer)は「常時 1 体」を既定にする**(2026-09-07、ユーザー指示)。
  ★**独立 worktree で走るので上の実装枠には入らない。**波の頭で通知・`TaskList` を見て
  **止まっていたら必ず配り直す**(前回の宿題と `meta-backlog.md` の先頭を渡す)。
  ★理由: 2026-09-07 の 1 日で本体が**道具の改善を 10 件その場で作った**が
  `meta-backlog.md` に**登録されていなかった** —— 数学が進むほどメタが後回しになる。
  ★**数学トラックが詰まっているかどうかと無関係に配る**(メタは数学の余りではない)。
- ゲート(`check.mjs --brief` / `lake build ABC3`)は
  **実装 agent が全員止まってから 1 回**。
- ★**同時数を上げると 1 往復が伸びる**(第 10 回の実測。agent 72 体、区間の重なりで数えた):

  | 同時 | 体数 | 1 tool あたり(中央) |
  |---|---|---|
  | 1 | 4 | **9.2 秒** |
  | 3 | 10 | 23.7 秒 |
  | 5 | 13 | 20.5 秒 |
  | 8 | 7 | **39.2 秒** |

  ★相関は **r = 0.215** と弱い(重い持ち場ほど長く走り、長く走るほど重なるため
  **交絡している**)。**「同時数を減らせば速くなる」の証明にはなっていない。**
- ★**token の費用は同時数では動かない。往復回数で動く**(第 10 回、`tools/session-cost.mjs`)。
  費用 ≒ **1 往復あたりの文脈(実測 547K token)× 往復回数**で、
  そのうち `cache_read` が **99.36%** である。同時数を変えても往復回数は変わらない。


## 4.5 持ち場を書く前に(2026-09-07 に本体が踏んだ失敗形)

### ★★★★特定のファイルを「これは CRLF だ」と**書き置かない**（2026-09-08、本体が 2 度誤った）

★**改行は `node tools/eol-audit.mjs --tracked` で、★その木で、★毎回自分で数える。**

☆★**本体の誤り 1**: 一つの隔離 worktree で測った一覧を「全体の事実」として agent に渡した。
★`core.autocrlf=true` かつ以前は `.gitattributes` が無かったので、
★**同じ commit でも作業ツリーによって改行が真逆**になる(メタ第 32 回の実測)。
☆★**本体の誤り 2**: ★**「`lean/ABC3/Found.lean` は CRLF」は誤りだった。**
★実装者が raw byte で測り直し、本体が追試した: ★**`CR 0 / LF 1836` —— LF である。**
★原因は ★**`grep -c $'\r'` がこの Bash ツールで展開されず全行にマッチする**こと
(`lean-idioms.md` #271 に逐語で入れた。★#141/#185 の記述は古い)。

★**正しい数え方**(どちらでもよい):
```
node tools/eol-audit.mjs --tracked --ext lean
node -e "const b=require('fs').readFileSync(F);let cr=0,lf=0;for(const c of b){if(c===13)cr++;if(c===10)lf++}console.log(cr,lf)"
```
★実測(2026-09-08、本体の木): `.lean` は **crlf 265 / lf 2042**、`tools/check.mjs` は **CRLF**、
★**`lean/ABC3/Found.lean` は LF**。★**この数字も「この木のこの時点」の話である。**

### ★★★★★MCP を使う agent を **持ち場に名指しする**（2026-09-08、メタ第 33 回の実測）

★★**基準環境は「無音で」すり替わる。**★**エラー文を 1 つも出さない。**

★実測: ★**すり替わり 11 件 / agent 9 体**。
★★**うち 9 件で「同じ imports を頼んだ別の agent がその瞬間に走っていた」と相手を名指しできた。**
★人手の記録(第 1077 の事件)と**独立に一致した**ので、検出器の裏取りも取れている。
☆★**2026-09-07 に 1 件 —— D27(「MCP は 1 体」)の承認より後にも起きている。**
★つまり ★**規約は既にあったが守られておらず、無音なので誰も気づかなかった。**

★また ★**`エラー: REPL は処理中(直列にしか使えない)` が 19 件**あった
（☆★改善係が事前登録した検出器は**英語しか知らず 0 件しか鳴らなかった**。
★**「0 件だから起きていない」と読みかけた**と自分で書いている）。
★その 18 件の瞬間の同時 MCP 利用者数は **15/18 が 2 本以上**。

★★**時間重みで見ると、lean-prover の同時 2 が走行時間の 54.2%** ——
★**過半の時間で D27 の天井に当たっている。**

⇒ ★★**持ち場を配るときに「あなたが MCP を使う側／使わない側」を必ず名指しする。**
- ★**MCP を使ってよいのは同時に 1 体だけ。**
- ★**他の agent は `node tools/leanfile.mjs`（1 往復 8〜11 秒）だけを使う。**
- ★**MCP 側にも「`lean_start` の直後に `lean_status` で imports を照合せよ」と書く。**
  ★**一致しなければそれが無音のすり替わりである。**★`lean_reset` せず `leanfile.mjs` に切り替える。
- ★**`lean_reset` は誰も打たない**（他のセッションを巻き込む）。

☆★**上限を 2 から 3 に上げてよい条件は 1 つだけ**: ★**3 本目が MCP を一切使わないこと。**
★**「速さ」では数字で言えない**（事前登録した 4 族はすべて Holm 後「言えない」、
実効 n は 22.5 しかない）。★**「壊れ」でだけ言える。**

☆★**説明できていない食い違いが 1 つある**（M167 に宿題として登録）:
★**上限は走行時間の 54.2% で天井に当たっている**のに、
★**同じ日の `frontier.mjs` では「保留でも空撃ちでもない着手可能」が 1 件**しかない。
★**供給の測り方が間違っていると、上限の議論そのものが空回りする。**

### ★★★`lake build` を **1 命令に 2 回書かない**（2026-09-08、メタ第 28 回の実測）

★★**`lake build` は本体の tool 待ちの最大項である（**41.6 時間**）。**
★木全部の build は **2,362 回 / 25.0 時間**、中央値 **34.8 秒**。
★★**対象を指定した build の中央値は 14.9 秒**（★**この 2 つを混ぜないこと**。
☆★2026-09-08 に本体は「中央値 29.9 秒」とだけ書き、★**主語が落ちて実装者が
自分の対象指定 build を 30 秒だと思う**書き方になっていた）。
★★**ゲート一式は `lake build` に比べれば小さい**（check + ledger + graph で合計 **4.52 時間**
—— ☆★**本体が最初に広めた「2.7 時間」は誤りで、独立に数え直すと 1.7 倍だった**）。
★**それでも 4.5 対 41.6 なので結論は変わらない: 削るなら `lake build`。**

★**機械で「無駄」と断定できたのは 3 つだけ = 合計 7.84 / 41.5 時間（**18.9%**、1 日あたり 36 分）**:
- 何も書かずに木全部を建て直し **503 回 / 4.5 時間**（☆★**これは上限**。子 agent のログが 2.2 日分しか無く、503 中 499 は「本当に誰も書いていない」を確かめられない）
- 同じくモジュール **343 回 / 2.8 時間**
- 1 命令の中で同じ対象を 2 度 **217 件 / 0.56 時間**

☆★★**訂正（メタ第 29 回）**: ★**一つ前の測定（M130）は過大だった。**
判定が Bash の命令文を素で見ており、★**本体が `cat > log.md <<'EOF' … lake build ABC3 … EOF` と
記録の本文に自分のコマンドを書き写すのを build 1 回と数えていた**
（しかも引数が読めず「引数なし＝木全部」に落ちていた）。
★叩いた命令 5,929 → **4,739**、木全部 4,089 → **2,362**、二重叩き 983 → **313**（同対象 **217**）。
★**「余分な build 5.1–6.4 時間」は誤りで、実測は 0.56 時間である。**
★★**教訓**: ★**自分の記録が自分の測定に混ざる。**★コマンドを数える道具は「実行されたか」と「書かれたか」を分けること。

```
×  lake build X 2>&1 | grep error; echo ---; lake build X 2>&1 | tail
○  lake build X > .cache/b.log 2>&1; grep -E 'error|sorry' .cache/b.log; tail -3 .cache/b.log
```

★**1 度だけ回してファイルに落とし、あとは grep する。**
★見たいものが 2 つあるなら、**build を 2 回ではなく grep を 2 回**にする。

### ★★★配線の前に **`import-audit.mjs --edge` を引く**（2026-09-08、循環を 2 度踏んだ後に作った）

```
node tools/import-audit.mjs --edge <引く側の .lean> <引き先のモジュール名>
```

★**0.4 秒で「循環するか」と「どの定義を `*Defs.lean` に割れば直るか」を返す。**
☆★**2026-09-07 に同じ形を 2 度踏んだ**（`Section1` → `Section1Defs`、`Section2` → `Section2Defs`）。
★**3 度目は予言済み**: `Found/PGC/RamificationJumpDivisibility.lean` → `Skeleton.PGC.Section4` は
★**循環する**（戻りの入口は `Skeleton.PGC.Section3`、直しは `Section4Defs.lean` に `RamificationFiltration.filt` を割る）。

### ★改善係の worktree は **`main` から切られていることがある**

★`main` は master より **1,210 commit 前**で、`tools/meta-setup.mjs` すら無い。
★**落ちたら即 `git reset --hard master`**（★第 28 回は 0.4 秒で復帰した）。
★持ち場の冒頭にこれを必ず書くこと。

### ★★★`grep sorry` は当てにならない（2026-09-07、agent を 1 度誤導した）

★**`lean/ABC3/Found/PGC/` を `grep sorry` すると 134 件出るが、★★全部 docstring の日本語**である（「sorry 無しで証明した」「sorry 無しで揃った」等）。
★**本物は 0 件である。**

```
×  grep -c sorry lean/ABC3/Found/PGC/*.lean
○  lake build ABC3 2>&1 | grep "declaration uses \`sorry\`"
```

☆★**実害**: 2026-09-07 に agent が
`Found/PGC/ArtinEquivariance.lean` を「`sorry` 3 件で未解決」と読み（★実際は **0 件**、3 件は「`Section1.lean` の `sorry` はまだ埋まっていない」という★**その日に古くなった地の文**）、
それを理由に原典の道を避けた。★道筋自体は良かったが、★**理由が事実と違っていた。**
★これはメタ第 26 回の「測って言えないと測れていないの取り違え」と同じ型である。

### ★★逐語引用に**波括弧を書かない**（2026-09-07、同じ NG が 6 件）

★**`leanQuoteProjection` が落とすのは `^` と `_` だけで、`{}` は落とさない。**
⇒ `K^{LT}` と書くと投影が `K{LT}` になり、PDF の `KLT` と食い違う。

```
×  > … K^{LT} …        → 投影 K{LT}
○  > … K^LT …          → 投影 KLT（PDF と一致）
```

★**添え字・肩字は波括弧なしで書く**（`µ^×_f,m`、`K^LT`）。
★これで落ちないときに初めて「語を引用から外す」を検討する。
★☆**2026-09-07 の実測**: NG 6 件のうち ★**6 件ともこれだけが原因**で、
★**語を外す必要は 1 件も無かった**（3 変種は全一致していた）。

### ★★★mathlib の**実パス**を必ず書く(2026-09-07、`find /` が 4 回投げられた)

```
D:\Math_ABC3\lean\.lake\packages\mathlib\Mathlib\
lean/.lake/packages/mathlib/Mathlib/   (Git Bash)
```

★★**`find /` を投げさせないこと。**★この環境では **120 秒で背景化され、費用だけかかって戻らない**。
★**2026-09-07 に 4 回投げられた**—— 本体は「索引を引け」とは書いていたが、★★**実パスを一度も書いていなかった**。
★agent は索引に無いものを見たいときに `find` に手が伸びる。★**道を示さない限り止まらない。**

★**順番**: ① `.cache/mathlib-index.txt` を awk-grep → ② 無ければ上の実パスを `ls` で確かめて `sed -n` → ③ `lean_check` の `#check`。
★索引と `.lake` の実ファイルは**同じ版**である。

☆★★★**2026-09-08 に 5 回目が出た。★この節を書いただけでは止まらなかった。**
実装 agent が `find / -name "Ultra.lean" -path "*Normed/Group*"` を投げ、
★**3 時間以上ぶら下がったまま残った**（本体が `TaskStop` で止めた。成果への影響は無し ——
同じ問いは索引への直接 grep で解決しており、結局 mathlib に頼らず抽象核を 6 行で自証していた）。

⇒ ★★**規約を「本体の義務」に変える**: ★**上の実パス 2 行を、持ち場（brief）の本文に毎回貼る。**
★「`autonomy-policy.md` を読め」では届かない —— ★**agent は持ち場に書いてあるものしか見ない。**
★同じ理由で、**`grep -rn ... .`（カレントからの再帰）も持ち場で禁じる**:
`external/`（FLT・mathlib のソース）と `lean/.lake/build/ir/*.setup.json`
（1 本 1.7 万行級が数百本）まで舐めるので桁違いに遅い。
★**範囲を書いた形を貼ること**（`ResearchPaper/ tools/` / `lean/ABC3/` のように）。

☆★**これは「配り方」の失敗であって agent の失敗ではない。**
★本体は 2026-09-07 に「実パスを一度も書いていなかった」と反省したのに、
★★**2026-09-08 の持ち場にもやはり貼っていなかった。**★同じ穴に 2 度落ちている。


★**`brief.mjs --paper <鍵> --id <id>` の出力は「冒頭から」読むこと。**
`sed` で節を絞るなら **必ず `head` を併用して冒頭も見る**。

★★**理由(実測)**: 2026-09-07 に本体が `sed -n '/## 1\./,/…/p'` で出力を絞り、
冒頭 8 行目の

```
★**この項目は既に木にある**: `Found/PGC/RamificationJumpDivisibility.lean`
```

を**切り落とした**。その結果 **既に 3 形で埋まっている `Corollary 6.3` を
「新規ファイルの持ち場」として配ってしまった**(実装者が 5 分で気づいて内容を組み替え、
別の価値 —— 原文が `hence` の 1 語で畳んだ部分 —— を出したので損失は小さかった)。

★**索引の grep も当たらなかった**: 本体は `e_?0` 等の**語**で引いたが、
実際の宣言名は `nat_card_quot_zero_dvd_of_ne` だった。
⇒ ☆**「在庫は名前ではなく型で引く」を本体自身が守っていなかった。**

**塞ぎ方(2 つとも入れた)**:
1. ★`brief.mjs` が **冒頭と末尾の両方**に警告を出すようにした(2026-09-07)。
   どちらか片方だけを読んでも当たる。
2. ★**持ち場を書く前に、既存ノードの有無を「結論の型」で 1 度引く。**
   語で引いて 0 件でも「無い」と結論しないこと。

★**同じ回にもう 1 つ**: 本体が在庫の対応表を書くとき
「`ramCoeff_smul_conj` が原文の `θ_n(στσ⁻¹) = u^n θ_n(τ)` に当たる」と書いたが**1 リンクずれていた**
(正しくは `residue_ramCoeff_conj`)。
⇒ ★★**持ち場に「実機で確かめていない。違ったら訂正して報告すること」と書いておくと覆る。**
2026-09-07 に **3 度**この形式で誤りが実装者の測定によって覆った
(#8 SubfieldClosed の 45 行 / `galoisUnitReciprocityMap` は Prop 4.4(iii) ではない /
`ramCoeff_smul_conj` の 1 リンクずれ)。
★**断定して渡すと誤りがそのまま Lean に入る。推測は推測として渡すこと。**

## 4.6 「越えられない」報告の扱い(2026-09-07 に観測した並列化の副作用)

★★**同時に 2 体を走らせると、片方の成果がもう片方の「越えられない」判定を無効化することがある。**

**実例(2026-09-07、Hasse-Arf)**: Y15 が Theorem 6.11 の 3 段のうち段 2 だけを落とし、
理由をこう報告した ——

> 段 2 — 入っていない。理由は**配管**(数学ではない): 帰納の各段が
> 「商 `G/H` が固定環 `C` に忠実に作用する」ことを要求し、木の Prop 6.9 / Lemma 6.10(ii) は
> その `C` と **7 本の適合条件**を仮定で受ける形になっている。

★**その 7 本を、Y16 が Y15 の走行中に供給していた**(`fixedRingMulSemiringAction` /
`algebraMap_smul_fixedRing` / `smul_fixedRing_eq_self` ほか、`FixedRingAction.lean` 213 行)。
⇒ ★**Y15 は Y16 を知らないまま止まった。** agent は起動時の木しか見えない。

**規則**:

- ★**「越えられない」「配管の限界」と報告された持ち場は、
  そのまま新ノードとして積まないこと。** まず
  **その波で他に着地したもの(および同時走行中のもの)を突き合わせる。**
- ★**突き合わせた結果「もう越えられるかもしれない」となったら、
  「本体は実機で確かめていない。確かめて、まだ足りなければ具体名で報告すること」
  と書いて即座に配り直す**(2026-09-07 に Y15b としてそうした)。
- ★**同時走行中のノードの成果を「待たせない」こと。**
  要るなら**仮定として受け取らせ**、着地したら差し込める形にさせる
  (Y13 が Lemma 6.10 に対してそうした。Y15b には Y17 の `hfix` に対してそうさせた)。
- ☆**これは並列化の損ではなく、配り手の仕事である。** agent 側では原理的に見えない。

## 4.7 ★★見当には `GUESS:` の 1 行を付ける(メタ第 14 回、backlog M45 → M50)

★**なぜ**: §4.5 の末尾は「推測は推測として渡すこと」で終わっているが、
**それが効いているかは今のところ測れない**。メタ第 13 回(M45)が測ろうとして
止まったのは、次の 1 点である ——

> 断りを書いた持ち場 21 件のうち覆ったのは 8 件(57%)。★しかし**断りを書かなかった**
> 持ち場でも覆っている(#16 の `pdfPage := 8`)。★★**覆らなかった誤りは記録に現れない**
> ので **分母が作れない**。

⇒ ★★**分母を作る唯一の方法は、断定して渡す見当も含めて全部に印を付けること。**

### 書式(2 行。`tools/unverified.mjs` が読む正典)

```
GUESS[<持ち場>]: <見当を 1 行で> | 確度=<高|中|低> | 検算=<型|実機|索引|原文|なし>
VERDICT[<持ち場>]: <当たり|外れ|半分|未確認> — <一言>
```

- `GUESS` は**配るとき**に `decisions-pending.md`(その波の節)へ書く。
  `VERDICT` は**報告が返ったとき**に、**同じ字面の鍵**で書く。
- `<持ち場>` は本体が既に使っている呼び名(`Y19e` / `#16` / `第1075波-3`)。
  1 つの持ち場に見当が複数あるなら `Y19e-a` `Y19e-b` と枝番を付ける。
- ★★**`確度=高` を省かないこと。** 自信のある見当を書かないと、
  「断定した見当が何件あって何件外れたか」が永遠に分からない ——
  それが M45 が当たった壁そのものである。今日の記録で本体が
  **18 回訂正されている**と自分で数えているのだから、`確度=高` の欄は必ず埋まる。
- ★これは**ゲートではない**。`check.mjs` に入れてはならない ——
  落とすようにすると「見当を書かない」ことで逃げられ、正直さを罰する
  (G7 の基準・G1 の `Found/` 非対称と同じ理由)。

### ★★★費用の自己申告には `COST:` の 1 行を付ける(メタ第 24 回 M103)

```
COST[<持ち場>]: <安|並|高>  — <一言>
COST[<鍵>]: <安|並|高> | 持ち場=<agent に配った呼び名>  — <一言>
```

★**なぜ**: メタ第 23 回が「思ったより安い/高いの自己申告は測れない」と結論した—— 散文で 21 節書かれていたが★**持ち場に紐づいたのは 1 件だけ**だった。

★★★**鍵は agent に配った呼び名にする。違うなら `| 持ち場=` を書く。**
★実測: `decisions-pending.md` の鍵 13 個のうち ★**agent の呼び名に当たるのは 2 個だけ**だった。
★**この 1 行が無いと、申告は書かれても実測に繋がらない。**

★数えるのは `node tools/agent-timing.mjs --cost`(★`unverified.mjs` ではない —— ★**実測を持つのは agent-timing だけ**であり、★`unverified.mjs` は Bonferroni の族を固定してあるので別種の欄を足すと m の意味が濫る)。

### ★★★「2 度目の覚き」を避ける —— 止め時を先に書く(メタ第 24 回 M102、★**本体の判断**)

★**実例**: 「抽象核の本数」は n=51 で Holm 0.0654(言えない) → n=52 で 0.0307(言える)に変わった。
★★**しかし盤面は動いていない** —— 旧の数字は 1 桁も違わずに再現し、★**閾を割らせたのは標本 +1 件だけ**だった。
⇒ ★★**これを「言える」として使わない。**★言えるのは「2 度目の覚きで閾を割った」までである。
★★**事前登録**: ★**「抽象核の本数」は n = 80 に達したときに 1 度だけ見る。**
★それまでは数字を見ても判定に使わない。

### 数え方

```
node tools/unverified.mjs            # 確度 × 当否の表(★M45 の分母)
node tools/unverified.mjs --open     # VERDICT 待ち
node tools/unverified.mjs --selftest # 書式を変えたら必ず走らせる(★34 件。Fisher と Bonferroni を含む)
```

### ★★★`GUESS` は「どう解くか」の見当であって、「何を解くか」の検算にはならない

★**2026-09-07 の実害**: 本体は Cor 4.9 の持ち場に
`K_π = K_{π′}` を配ったが、★★**これは数学的に偽**だった
（反例: `K = ℚ_p`（p 奇）、`π = p`、`π′ = -p`。真の形は `K_π ⊔ K^ur = K_{π′} ⊔ K^ur`）。
★**その波で本体は `GUESS` を 4 件書いていたが、4 件のどれにもこの誤りは現れていない** ——
見当はすべて「どう解くか」についてだったからである。

⇒ ★★**持ち場を書く前に、statement の**射程**を原典で確かめること。**
★★★**`brief.mjs` の 1b が完走していても安全の保証にはならない** ——
上の例では、射程を決める一文が**項目の外**（直後の Definition 4.10）にあり、
1b は Cor 4.9 の Proof で**正しく**止まるので原理的に入らなかった。
実装者が `.txt` を `grep -a` + `sed` で直読して見つけ、真の形に直した。

★**手順**: `確度=高` を書く前に、その項目の**直後 20 行**を `.txt` で見る
（`set X :=` / `we write` / `Definition` / `In the sequel` のような射程を決める言い回しを探す）。

★**いつから比べられるか**: `確度=高` と `確度=中/低` の**両方**に判定済みが
溜まってから。★**遡って印を付けることはできない**(メタ第 14 回に実測:
自然言語の断りを grep すると `decisions-pending.md` 全体で **72 行**当たるが、
M45 が手で数えた 1 日ぶんは **21 件**で、しかも**行から持ち場が決まらない**)。
⇒ ★**分母は「この規約を入れた次の波」から始まる。**

### ★★★★配る前に **指数・定数の「総和」が結論と合うかを 1 度だけ検算する**（2026-09-08、4 例目）

★**今日だけで「配った目標が偽」が 4 回起きている**:
`K_π = K_{π′}` / `colim_S H¹(S,A) = 0` / 「`Σ i_j/e_j` の収束」/
★「`c k ≤ p^{(1/(p−1))(1/p)^k}`」。
★**うち 2 回は「前の波の申し送りをそのまま渡した」ものである。**

☆★**4 例目は機械的に検出できた**: 仮説が要求する指数の総和
`Σ_{k≥1} (1/(p−1))·p^{−k} = 1/(p−1)²` が、★結論が要求する `p/(p−1)²` **より小さい**。
⇒ 仮説は結論を出すには弱すぎ、★かつ実際の反例（`K = ℚ_p(ζ_p)`, `x = p^{1/p}`）が
`c 1 ≥ p^{1/(p−1)}` を強制するので、★**その仮説を満たす `c` は存在しない = 定理は空虚**だった。
★正しい指数 `(1/p)^{k−1}` は総和がちょうど `p/(p−1)²` で一致し、
★`axDecay p 1 = p^{1/(p−1)}` は**古典的な最良定数**とも一致する。

⇒ ★★**手順（持ち場を書く直前、30 秒）**:
1. ★配る不等式・漸化式に**指数や定数の族**（`c k`, `ε_k`, `i_j` 等）が出てきたら、
   ★**総和 / 総積を閉じた形で書き、結論が要求する量と突き合わせる。**
2. ★**`k = 1`（あるいは最小の段）を具体的な体で 1 例だけ手計算する。**
   ★`K = ℚ_p`, `K = ℚ_p(ζ_p)`, `x = p^{1/p}` の 3 つで足りることが多い。
3. ★**古典的な最良定数が知られているなら、それと一致するかを見る。**
   ★一致しないなら、★**ずれている方が誤り**である可能性を先に疑う。

★★**これは `GUESS` では捕まらない** —— 見当は「どう解くか」についてであり、
★上の 3 手は「**何を解くか**」の検算だからである（前節と同じ理由）。

★★★**申し送りは検算の免除にならない**: 前の波が名指しした次のノードであっても、
★**配る前にこの 3 手を必ず通す。**★今回はそれで気づけたはずだった。

## 5. 報告の義務

無人で回した波も、**何を判断し何を保留したか**を残す:

- commit メッセージに測定値(ジョブ数・NG 件数)を書く
- 保留した判断は `decisions-pending.md` に理由つきで
- メタの測定は `meta-backlog.md` に書き戻す
- 在庫の測定は `memory/` に(★「不在」の測定は**再実行できる形**で。
  2026-09-05 に 4 件が覆った)
